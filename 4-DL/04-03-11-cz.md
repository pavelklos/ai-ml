# Multimodální hluboké učení

## Obsah
1. Úvod do multimodálního hlubokého učení
2. Strategie fúze dat
3. Multimodální architektury
4. Modely pro práci s obrázky a textem
5. Audiovizuální modely
6. Multimodální transformery
7. Učení multimodálních reprezentací
8. Techniky a výzvy při tréninku
9. Aplikace a případy užití
10. Shrnutí

## 1. Úvod do multimodálního hlubokého učení

Multimodální hluboké učení označuje modely, které dokáží zpracovávat a propojovat informace z různých typů datových zdrojů (modalit), jako jsou obrázky, text, zvuk a video. Na rozdíl od tradičních přístupů hlubokého učení, které se zaměřují na jedinou modalitu, multimodální systémy se učí chápat korelace a vztahy mezi různými modalitami, aby mohly lépe predikovat nebo generovat vylepšené výstupy.

```python
# Instalace běžných balíčků pro multimodální hluboké učení
!pip install torch torchvision transformers pillow matplotlib numpy tqdm pandas scipy librosa soundfile

# Import základních knihoven
import torch
import torch.nn as nn
import torch.optim as optim
import torchvision
import transformers
import numpy as np
import matplotlib.pyplot as plt
import pandas as pd
from PIL import Image

# Kontrola dostupnosti GPU
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print(f"Používané zařízení: {device}")
```

Hlavní výhody multimodálního učení zahrnují:

- **Doplňující se informace**: Různé modality často poskytují doplňující se informace, což vede k robustnějším reprezentacím
- **Multimodální učení**: Schopnost přenosu znalostí z jedné modality do druhé
- **Zpracování chybějících dat**: Schopnost provádět predikce i když některé modality nejsou k dispozici
- **Zvýšený výkon**: Využívání více modalit často vede k lepším výsledkům než přístupy s jedinou modalitou

## 2. Strategie fúze dat

Multimodální systémy využívají různé strategie pro kombinaci informací z různých modalit. Hlavními přístupy jsou:

1. **Raná fúze (Early Fusion)**: Kombinování surových příznaků z různých modalit před zpracováním
2. **Pozdní fúze (Late Fusion)**: Zpracování každé modality zvlášť a kombinování jejich predikcí
3. **Průběžná fúze (Intermediate Fusion)**: Propojování reprezentací na různých úrovních zpracování

```python
class EarlyFusionModel(nn.Module):
    def __init__(self, image_features, text_features, fusion_output_size, num_classes):
        super(EarlyFusionModel, self).__init__()
        
        # Extraktory příznaků pro každou modalitu
        self.image_encoder = torchvision.models.resnet18(pretrained=True)
        self.image_encoder.fc = nn.Linear(self.image_encoder.fc.in_features, image_features)
        
        self.text_encoder = nn.Sequential(
            nn.Embedding(10000, 128),  # Předpokládáme slovník o velikosti 10000
            nn.LSTM(128, text_features, batch_first=True),
        )
        
        # Raná fúze (konkatenace)
        self.fusion_layer = nn.Sequential(
            nn.Linear(image_features + text_features, fusion_output_size),
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(fusion_output_size, num_classes)
        )
    
    def forward(self, image, text):
        # Extrakce příznaků z každé modality
        image_features = self.image_encoder(image)
        text_output, (text_hidden, _) = self.text_encoder(text)
        text_features = text_hidden[-1]  # Použití posledního skrytého stavu
        
        # Konkatenace příznaků (raná fúze)
        combined_features = torch.cat([image_features, text_features], dim=1)
        
        # Predikce na základě sloučených příznaků
        output = self.fusion_layer(combined_features)
        return output

class LateFusionModel(nn.Module):
    def __init__(self, image_features, text_features, num_classes):
        super(LateFusionModel, self).__init__()
        
        # Extraktory příznaků pro každou modalitu
        self.image_encoder = torchvision.models.resnet18(pretrained=True)
        self.image_encoder.fc = nn.Linear(self.image_encoder.fc.in_features, num_classes)
        
        self.text_encoder = nn.Sequential(
            nn.Embedding(10000, 128),
            nn.LSTM(128, text_features, batch_first=True),
            nn.Linear(text_features, num_classes)
        )
    
    def forward(self, image, text):
        # Získání predikcí z každé modality
        image_pred = self.image_encoder(image)
        
        # Pro text potřebujeme extrahovat predikci z výstupu LSTM
        text_output, (text_hidden, _) = self.text_encoder[:-1](text)
        text_pred = self.text_encoder[-1](text_hidden[-1])
        
        # Kombinace predikcí (jednoduché průměrování pro pozdní fúzi)
        combined_pred = (image_pred + text_pred) / 2
        return combined_pred
```

## 3. Multimodální architektury

Multimodální architektury jsou speciálně navrženy pro zpracování a integraci informací z více modalit. Klíčové architektonické komponenty zahrnují:

1. **Enkodéry specifické pro jednotlivé modality**: Specializované sítě, které převádějí surové vstupy z každé modality do embedingů
2. **Multimodální pozornostní mechanismy**: Mechanismy umožňující jedné modalitě zaměřit se na relevantní části jiné modality
3. **Společné reprezentační prostory**: Společné prostory reprezentací, kde mohou být porovnávány příznaky z různých modalit

```python
class CrossModalAttention(nn.Module):
    def __init__(self, image_dim, text_dim, hidden_dim):
        super(CrossModalAttention, self).__init__()
        
        # Projekce pro dotaz a klíč
        self.image_projection = nn.Linear(image_dim, hidden_dim)
        self.text_projection = nn.Linear(text_dim, hidden_dim)
        self.output_projection = nn.Linear(image_dim + text_dim, hidden_dim)
        
    def forward(self, image_features, text_features):
        # Projekce obrazových a textových příznaků
        image_proj = self.image_projection(image_features)
        text_proj = self.text_projection(text_features)
        
        # Výpočet vah pozornosti
        attention_weights = torch.matmul(image_proj, text_proj.transpose(-2, -1)) / (hidden_dim ** 0.5)
        attention_weights = torch.softmax(attention_weights, dim=-1)
        
        # Aplikace pozornosti
        context_vector = torch.matmul(attention_weights, text_features)
        
        # Konkatenace s původními obrazovými příznaky
        combined = torch.cat([image_features, context_vector], dim=-1)
        output = self.output_projection(combined)
        
        return output

class MultiModalArchitecture(nn.Module):
    def __init__(self, image_dim=2048, text_dim=768, hidden_dim=512, num_classes=10):
        super(MultiModalArchitecture, self).__init__()
        
        # Enkodér obrázků (ResNet)
        self.image_encoder = torchvision.models.resnet50(pretrained=True)
        # Odstranění poslední klasifikační vrstvy
        self.image_encoder = nn.Sequential(*list(self.image_encoder.children())[:-1])
        
        # Enkodér textu (BERT)
        self.text_encoder = transformers.BertModel.from_pretrained('bert-base-uncased')
        
        # Multimodální pozornostní mechanismus
        self.cross_attention = CrossModalAttention(image_dim, text_dim, hidden_dim)
        
        # Finální klasifikátor
        self.classifier = nn.Sequential(
            nn.Linear(hidden_dim, hidden_dim // 2),
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(hidden_dim // 2, num_classes)
        )
        
    def forward(self, image, input_ids, attention_mask):
        # Zpracování obrázku
        image_features = self.image_encoder(image).squeeze()
        
        # Zpracování textu
        text_output = self.text_encoder(input_ids=input_ids, attention_mask=attention_mask)
        text_features = text_output.last_hidden_state
        
        # Multimodální pozornost
        fused_features = self.cross_attention(image_features, text_features)
        
        # Klasifikace
        output = self.classifier(fused_features)
        
        return output
```

## 4. Modely pro práci s obrázky a textem

Modely pro práci s obrázky a textem patří mezi nejrozšířenější multimodální architektury, navržené pro pochopení vztahů mezi vizuálními a textovými informacemi. Mezi příklady patří:

- **CLIP (Contrastive Language-Image Pretraining)**: Učí vizuální koncepty z přirozeného jazykového dohledu
- **VilBERT**: Vision-and-Language BERT, rozšíření modelu BERT pro zpracování vizuálních i textových vstupů
- **LXMERT**: Learning Cross-Modality Encoder Representations from Transformers

```python
# Příklad použití modelu CLIP pro zero-shot klasifikaci obrázků
import torch
from PIL import Image
import requests
from transformers import CLIPProcessor, CLIPModel

# Načtení modelu a procesoru
model = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")
processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")

# Načtení obrázku
url = "http://images.cocodataset.org/val2017/000000039769.jpg"
image = Image.open(requests.get(url, stream=True).raw)

# Definice kandidátních popisků
candidate_labels = ["fotografie kočky", "fotografie psa", "fotografie koně"]

# Zpracování vstupů
inputs = processor(
    text=candidate_labels,
    images=image,
    return_tensors="pt",
    padding=True
)

# Získání predikcí modelu
with torch.no_grad():
    outputs = model(**inputs)

# Výpočet pravděpodobností
logits_per_image = outputs.logits_per_image  # skóre podobnosti obrázek-text
probs = logits_per_image.softmax(dim=1)  # pravděpodobnosti

# Výpis výsledků
for label, prob in zip(candidate_labels, probs[0]):
    print(f"{label}: {prob.item():.3f}")
```

### Implementace generování popisků k obrázkům

Častým úkolem při práci s obrázky a textem je generování popisků k obrázkům:

```python
# Jednoduchý model pro generování popisků k obrázkům
class ImageCaptioningModel(nn.Module):
    def __init__(self, embed_size, hidden_size, vocab_size, num_layers=1):
        super(ImageCaptioningModel, self).__init__()
        
        # CNN pro extrakci příznaků z obrázku
        resnet = torchvision.models.resnet50(pretrained=True)
        modules = list(resnet.children())[:-1]
        self.resnet = nn.Sequential(*modules)
        
        # Lineární vrstva pro konverzi obrazových příznaků na embedding
        self.image_embed = nn.Linear(resnet.fc.in_features, embed_size)
        
        # LSTM generátor popisků
        self.embed = nn.Embedding(vocab_size, embed_size)
        self.lstm = nn.LSTM(embed_size, hidden_size, num_layers, batch_first=True)
        self.linear = nn.Linear(hidden_size, vocab_size)
        
    def forward(self, images, captions):
        # Extrakce příznaků z obrázku
        with torch.no_grad():
            features = self.resnet(images)
        features = features.reshape(features.size(0), -1)
        
        # Embedování obrazových příznaků
        features = self.image_embed(features)
        
        # Embedování popisků
        embeddings = self.embed(captions)
        
        # Konkatenace příznaků obrázků a embedingů popisků
        # Obrazové příznaky jsou použity jako první vstup do LSTM (jako token <START>)
        embeddings = torch.cat((features.unsqueeze(1), embeddings), dim=1)
        
        # Dopředný průchod LSTM
        lstm_out, _ = self.lstm(embeddings)
        outputs = self.linear(lstm_out)
        
        return outputs
```

## 5. Audiovizuální modely

Audiovizuální modely zpracovávají současně zvukové a vizuální vstupy, aby plnily úkoly jako:
- Audiovizuální rozpoznávání řeči
- Lokalizace zdroje zvuku
- Detekce audiovizuálních událostí
- Analýza hudebních videí

```python
import torch
import torch.nn as nn
import torchvision.models as models
import torchaudio.models as audio_models

class AudioVisualModel(nn.Module):
    def __init__(self, num_classes):
        super(AudioVisualModel, self).__init__()
        
        # Extraktor vizuálních příznaků
        self.visual_encoder = models.resnet18(pretrained=True)
        self.visual_encoder.fc = nn.Identity()  # Odstranění klasifikační vrstvy
        visual_feature_dim = 512  # Výstupní dimenze ResNet18
        
        # Extraktor zvukových příznaků
        self.audio_encoder = audio_models.wav2vec2_model.Wav2Vec2Model.from_pretrained("facebook/wav2vec2-base")
        audio_feature_dim = 768  # Výstupní dimenze wav2vec2
        
        # Fúze a klasifikace
        fusion_dim = visual_feature_dim + audio_feature_dim
        self.classifier = nn.Sequential(
            nn.Linear(fusion_dim, 512),
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(512, num_classes)
        )
    
    def forward(self, visual_input, audio_input):
        # Extrakce vizuálních příznaků
        visual_features = self.visual_encoder(visual_input)
        
        # Extrakce zvukových příznaků
        audio_features = self.audio_encoder(audio_input).last_hidden_state.mean(dim=1)
        
        # Konkatenace příznaků
        combined_features = torch.cat([visual_features, audio_features], dim=1)
        
        # Klasifikace
        output = self.classifier(combined_features)
        return output

# Příklad tréninku audiovizuálního modelu
def train_audio_visual_model(model, train_loader, criterion, optimizer, num_epochs):
    model.train()
    for epoch in range(num_epochs):
        running_loss = 0.0
        for visual_inputs, audio_inputs, labels in train_loader:
            visual_inputs = visual_inputs.to(device)
            audio_inputs = audio_inputs.to(device)
            labels = labels.to(device)
            
            # Dopředný průchod
            outputs = model(visual_inputs, audio_inputs)
            loss = criterion(outputs, labels)
            
            # Zpětný průchod a optimalizace
            optimizer.zero_grad()
            loss.backward()
            optimizer.step()
            
            running_loss += loss.item()
            
        print(f'Epocha {epoch+1}/{num_epochs}, Ztráta: {running_loss/len(train_loader)}')
```

## 6. Multimodální transformery

Architektury transformerů zrevolučnily multimodální učení tím, že umožnily efektivní mechanismy pozornosti napříč různými modalitami. Mezi významné multimodální transformery patří:

- **ViLT**: Vision-and-Language Transformer
- **FLAVA**: Model pro základní propojení jazyka a vidění
- **CLIP**: Contrastive Language-Image Pre-training
- **VisualBERT**: Model založený na BERTu pro vizuálně-jazykové úlohy

```python
from transformers import ViltProcessor, ViltForQuestionAnswering
import requests
from PIL import Image

# Načtení modelu a procesoru pro zodpovídání vizuálních otázek
processor = ViltProcessor.from_pretrained("dandelin/vilt-b32-finetuned-vqa")
model = ViltForQuestionAnswering.from_pretrained("dandelin/vilt-b32-finetuned-vqa")

# Příprava vstupů
url = "http://images.cocodataset.org/val2017/000000039769.jpg"
image = Image.open(requests.get(url, stream=True).raw)
text = "Kolik koček je na obrázku?"

# Zpracování vstupů
inputs = processor(image, text, return_tensors="pt")

# Dopředný průchod
with torch.no_grad():
    outputs = model(**inputs)

# Zpracování výstupů
logits = outputs.logits
idx = logits.argmax(-1).item()
print("Odpověď:", model.config.id2label[idx])
```

Vytvoření vlastního multimodálního transformeru:

```python
class MultiModalTransformer(nn.Module):
    def __init__(self, image_encoder, text_encoder, num_classes):
        super(MultiModalTransformer, self).__init__()
        
        # Načtení předtrénovaného enkodéru obrázků
        self.image_encoder = image_encoder
        
        # Načtení předtrénovaného textového enkodéru
        self.text_encoder = text_encoder
        
        # Vrstvy pro multimodální pozornost
        self.image_to_text_attention = nn.MultiheadAttention(768, 8)
        self.text_to_image_attention = nn.MultiheadAttention(768, 8)
        
        # Finální predikční vrstva
        self.classifier = nn.Linear(768 * 2, num_classes)
        
    def forward(self, pixel_values, input_ids, attention_mask):
        # Získání obrazových příznaků
        image_outputs = self.image_encoder(pixel_values)
        image_features = image_outputs.last_hidden_state
        
        # Získání textových příznaků
        text_outputs = self.text_encoder(input_ids=input_ids, attention_mask=attention_mask)
        text_features = text_outputs.last_hidden_state
        
        # Multimodální pozornost (obraz se zaměřuje na text)
        image_attend_text, _ = self.image_to_text_attention(
            image_features.permute(1, 0, 2),
            text_features.permute(1, 0, 2),
            text_features.permute(1, 0, 2),
            key_padding_mask=~attention_mask.bool()
        )
        
        # Multimodální pozornost (text se zaměřuje na obraz)
        text_attend_image, _ = self.text_to_image_attention(
            text_features.permute(1, 0, 2),
            image_features.permute(1, 0, 2),
            image_features.permute(1, 0, 2)
        )
        
        # Návrat k formátu batch-first a výpočet průměru
        image_attend_text = image_attend_text.permute(1, 0, 2).mean(dim=1)
        text_attend_image = text_attend_image.permute(1, 0, 2).mean(dim=1)
        
        # Konkatenace
        fused_features = torch.cat([image_attend_text, text_attend_image], dim=1)
        
        # Klasifikace
        output = self.classifier(fused_features)
        
        return output
```

## 7. Učení multimodálních reprezentací

Učení multimodálních reprezentací má za cíl vytvořit sdílené embedingy, kde jsou sémanticky podobné obsahy z různých modalit mapovány do blízkých bodů v prostoru embedingů. To umožňuje úlohy jako multimodální vyhledávání a zero-shot učení.

```python
class ContrastiveLoss(nn.Module):
    def __init__(self, temperature=0.07):
        super(ContrastiveLoss, self).__init__()
        self.temperature = temperature
        
    def forward(self, image_embeddings, text_embeddings):
        # Normalizace embedingů
        image_embeddings = nn.functional.normalize(image_embeddings, p=2, dim=1)
        text_embeddings = nn.functional.normalize(text_embeddings, p=2, dim=1)
        
        # Výpočet matice podobnosti
        logits = torch.matmul(image_embeddings, text_embeddings.T) / self.temperature
        
        # Štítky jsou diagonální prvky (spárované obrázky a texty)
        labels = torch.arange(logits.size(0)).to(logits.device)
        
        # Výpočet ztráty v obou směrech (obrázek->text a text->obrázek)
        image_to_text_loss = nn.functional.cross_entropy(logits, labels)
        text_to_image_loss = nn.functional.cross_entropy(logits.T, labels)
        
        # Celková ztráta
        total_loss = (image_to_text_loss + text_to_image_loss) / 2
        return total_loss

# Příklad tréninku s kontrastivní ztrátou
class DualEncoder(nn.Module):
    def __init__(self, embedding_dim=512):
        super(DualEncoder, self).__init__()
        
        # Enkodér obrázků
        self.image_encoder = torchvision.models.resnet50(pretrained=True)
        self.image_encoder.fc = nn.Linear(2048, embedding_dim)
        
        # Textový enkodér
        self.text_encoder = transformers.BertModel.from_pretrained('bert-base-uncased')
        self.text_projection = nn.Linear(768, embedding_dim)
        
    def forward_image(self, images):
        image_features = self.image_encoder(images)
        return image_features
    
    def forward_text(self, input_ids, attention_mask):
        text_output = self.text_encoder(input_ids=input_ids, attention_mask=attention_mask)
        text_embeddings = text_output.pooler_output
        text_embeddings = self.text_projection(text_embeddings)
        return text_embeddings

# Trénovací smyčka pro duální enkodér
def train_dual_encoder(model, train_loader, optimizer, num_epochs):
    criterion = ContrastiveLoss()
    model.train()
    
    for epoch in range(num_epochs):
        running_loss = 0.0
        for images, input_ids, attention_mask in train_loader:
            images = images.to(device)
            input_ids = input_ids.to(device)
            attention_mask = attention_mask.to(device)
            
            # Dopředný průchod
            image_embeddings = model.forward_image(images)
            text_embeddings = model.forward_text(input_ids, attention_mask)
            
            # Výpočet ztráty
            loss = criterion(image_embeddings, text_embeddings)
            
            # Zpětný průchod a optimalizace
            optimizer.zero_grad()
            loss.backward()
            optimizer.step()
            
            running_loss += loss.item()
            
        print(f'Epocha {epoch+1}/{num_epochs}, Ztráta: {running_loss/len(train_loader)}')
```

## 8. Techniky a výzvy při tréninku

Trénink modelů multimodálního hlubokého učení přináší jedinečné výzvy:

1. **Rozdílná dynamika učení**: Modality se mohou učit různým tempem
2. **Dominance modality**: Jedna modalita může převažovat nad ostatními v procesu učení
3. **Chybějící data**: Zvládání scénářů, kdy jedna nebo více modalit chybí
4. **Problémy se synchronizací**: Zajištění správné synchronizace mezi modalitami
5. **Výpočetní nároky**: Multimodální modely často vyžadují značný výpočetní výkon

```python
# Technika vyvážení gradientů pro multimodální učení
class GradientBalancer:
    def __init__(self, model, modalities=['image', 'text', 'audio']):
        self.model = model
        self.modalities = modalities
        self.running_grad_norms = {mod: 1.0 for mod in modalities}
        self.alpha = 0.01  # Rychlost aktualizace klouzavých průměrů
    
    def backward_and_balance(self, loss):
        # Výpočet gradientů
        loss.backward(retain_graph=True)
        
        # Získání gradientů pro různé komponenty
        grad_norms = {}
        for mod in self.modalities:
            mod_params = getattr(self.model, f"{mod}_encoder").parameters()
            mod_grad = torch.cat([p.grad.flatten() for p in mod_params if p.grad is not None])
            grad_norms[mod] = torch.norm(mod_grad).item()
        
        # Aktualizace klouzavých průměrů
        for mod in self.modalities:
            self.running_grad_norms[mod] = (1 - self.alpha) * self.running_grad_norms[mod] + self.alpha * grad_norms[mod]
        
        # Výpočet škálovacích faktorů
        max_norm = max(self.running_grad_norms.values())
        scale_factors = {mod: max_norm / norm if norm > 0 else 1.0 
                         for mod, norm in self.running_grad_norms.items()}
        
        # Škálování gradientů
        for mod in self.modalities:
            mod_params = getattr(self.model, f"{mod}_encoder").parameters()
            for p in mod_params:
                if p.grad is not None:
                    p.grad *= scale_factors[mod]

# Zpracování chybějících modalit během tréninku
class MissingModalityTrainer:
    def __init__(self, model, modality_dropout_prob=0.3):
        self.model = model
        self.modality_dropout_prob = modality_dropout_prob
    
    def forward_with_dropout(self, **inputs):
        # Náhodné vyřazení modalit během tréninku
        if self.model.training:
            for modality in inputs:
                if np.random.random() < self.modality_dropout_prob:
                    # Nahrazení nulami nebo naučeným tokenem
                    if isinstance(inputs[modality], torch.Tensor):
                        inputs[modality] = torch.zeros_like(inputs[modality])
        
        # Dopředný průchod s potenciálně vyřazenými modalitami
        return self.model(**inputs)
```

## 9. Aplikace a případy užití

Multimodální hluboké učení má řadu praktických aplikací v různých oblastech:

1. **Zdravotnictví**: Kombinace medicínských snímků s textovými záznamy pacientů pro diagnostiku
2. **Autonomní vozidla**: Integrace dat z kamery, lidaru a radaru pro vnímání
3. **Virtuální asistenti**: Zpracování hlasových příkazů i vizuálního kontextu
4. **E-commerce**: Multimodální vyhledávání a doporučování produktů
5. **Sociální sítě**: Porozumění obsahu napříč více modalitami
6. **Dostupnost**: Konverze obsahu mezi modalitami pro lepší přístupnost (např. popisky obrázků)

```python
# Příklad: Multimodální model pro zdravotnictví kombinující zobrazovací a pacientská data
class MedicalMultiModalModel(nn.Module):
    def __init__(self, num_classes):
        super(MedicalMultiModalModel, self).__init__()
        
        # Enkodér medicínských snímků (např. rentgenů, CT)
        self.image_encoder = torchvision.models.densenet121(pretrained=True)
        self.image_encoder.classifier = nn.Identity()
        
        # Enkodér dat pacientů (pro tabulární data jako věk, životní funkce, laboratorní výsledky)
        self.tabular_encoder = nn.Sequential(
            nn.Linear(50, 128),  # Předpokládáme 50 příznaků
            nn.ReLU(),
            nn.BatchNorm1d(128),
            nn.Dropout(0.3),
            nn.Linear(128, 64)
        )
        
        # Textový enkodér (pro klinické poznámky)
        self.text_encoder = transformers.BertModel.from_pretrained('bert-base-uncased')
        self.text_projection = nn.Linear(768, 64)
        
        # Fúze a klasifikace
        self.fusion = nn.Sequential(
            nn.Linear(1024 + 64 + 64, 256),  # DenseNet: 1024, tabulární: 64, text: 64
            nn.ReLU(),
            nn.Dropout(0.5),
            nn.Linear(256, num_classes)
        )
    
    def forward(self, images, tabular_data, input_ids, attention_mask):
        # Zpracování medicínských snímků
        image_features = self.image_encoder(images)
        
        # Zpracování tabulárních dat pacientů
        tabular_features = self.tabular_encoder(tabular_data)
        
        # Zpracování klinických poznámek
        text_outputs = self.text_encoder(input_ids=input_ids, attention_mask=attention_mask)
        text_features = self.text_projection(text_outputs.pooler_output)
        
        # Kombinace všech příznaků
        combined = torch.cat([image_features, tabular_features, text_features], dim=1)
        
        # Predikce diagnózy
        output = self.fusion(combined)
        return output

# Příklad: Systém vnímání pro autonomní vozidla
class AutonomousVehiclePerception(nn.Module):
    def __init__(self, num_classes):
        super(AutonomousVehiclePerception, self).__init__()
        
        # Enkodér dat z kamery
        self.camera_encoder = torchvision.models.efficientnet_b0(pretrained=True)
        self.camera_encoder.classifier = nn.Identity()
        camera_features = 1280  # EfficientNet B0 příznaky
        
        # Enkodér bodového mračna z lidaru
        self.lidar_encoder = nn.Sequential(
            # Zjednodušená architektura typu PointNet
            nn.Linear(3, 64),  # (x,y,z) souřadnice
            nn.ReLU(),
            nn.Linear(64, 128),
            nn.ReLU(),
            nn.Linear(128, 256)
        )
        
        # Enkodér dat z radaru
        self.radar_encoder = nn.Sequential(
            nn.Conv2d(1, 16, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(2),
            nn.Conv2d(16, 32, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(2),
            nn.Flatten(),
            nn.Linear(32 * 16 * 16, 128)  # Předpokládáme vstup radaru 64x64
        )
        
        # Fúzní síť
        self.fusion = nn.Sequential(
            nn.Linear(camera_features + 256 + 128, 512),
            nn.ReLU(),
            nn.Dropout(0.4),
            nn.Linear(512, 256),
            nn.ReLU(),
            nn.Linear(256, num_classes)
        )
    
    def forward(self, camera_data, lidar_data, radar_data):
        # Zpracování dat z různých senzorů
        camera_features = self.camera_encoder(camera_data)
        lidar_features = self.lidar_encoder(lidar_data)
        radar_features = self.radar_encoder(radar_data)
        
        # Fúze příznaků
        combined = torch.cat([camera_features, lidar_features, radar_features], dim=1)
        
        # Detekce/klasifikace objektů
        output = self.fusion(combined)
        return output
```

## 10. Shrnutí

Multimodální hluboké učení představuje významný pokrok ve schopnosti umělé inteligence chápat a zpracovávat informace způsobem podobnějším lidskému vnímání. Integrací více datových zdrojů tyto modely získávají komplexnější pochopení svého prostředí.

Klíčové poznatky:

1. **Strategie fúze** (raná, pozdní a průběžná) poskytují různé přístupy ke kombinování informací z různých modalit.

2. **Multimodální architektury** se vyvinuly od jednoduchých přístupů založených na konkatenaci až po sofistikované modely na bázi transformerů s mechanismy multimodální pozornosti.

3. **Učení multimodálních reprezentací** umožňuje výkonné schopnosti jako zero-shot učení a multimodální vyhledávání.

4. **Výzvy při tréninku** zahrnují vyvažování gradientů mezi modalitami, zpracování chybějících dat a výpočetní složitost.

5. **Aplikace** zahrnují mnoho oblastí včetně zdravotnictví, autonomních vozidel, virtuálních asistentů, e-commerce a sociálních médií.

S tím, jak se senzory a datové zdroje nadále rozšiřují, bude multimodální hluboké učení hrát stále důležitější roli při vývoji AI systémů, které dokáží chápat a interagovat se světem přirozenějším a komplexnějším způsobem.

Budoucí směry v multimodálním hlubokém učení zahrnují:
- Efektivnější předtrénování na masivních, rozmanitých multimodálních datasetech
- Lepší zpracování chybějících nebo zašuměných modalit
- Vylepšené techniky multimodálního propojení
- Transfer učení napříč různorodějšími modalitami
- Vývoj modelů, které dokáží dynamicky rozhodovat, kterým modalitám věnovat pozornost